# Django settings (secret key left as you requested)
DJANGO_SECRET_KEY="5$h-@2f2)wuzna1tnu!+#8=4_%%wu7g@m-8r+69t(+hw5-)p_6"
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,.onrender.com

# Use local Postgres for development
USE_LOCAL_POSTGRES=True

# Local Postgres config (you asked to put actual local DB details here)
LOCAL_DB_NAME=events_db
LOCAL_DB_USER=events_user
LOCAL_DB_PASSWORD=events_pass
LOCAL_DB_HOST=localhost
LOCAL_DB_PORT=5432

# Also set DATABASE_URL to local (keeps compatibility)
DATABASE_URL=postgresql://events_user:events_pass@localhost:5432/events_db
DATABASE_SSL_REQUIRE=False

# Gmail SMTP settings (left unchanged as requested)
GMAIL_EMAIL=trackev3@gmail.com
GMAIL_PASSWORD=zdedxseqefolkmlf
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# Time zone
TIME_ZONE=UTC
